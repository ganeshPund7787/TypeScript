1) TypeScript has static cheacking && ts is rapper of js to allowend write code errorless

2) TypeScript analyze the code that we have write and find the error in development time 

3) TypeScript is development tool Which is use to development;

4) At the end our code translate in the js

5) if your variable type is any that is not good practice

6) we never set set (setter) type in ts 

7) access modifire => public & private
